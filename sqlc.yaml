version: "1"
project:
    id: ""
packages:
    -   name: "db"
        path: "./sql/query/"
        schema: "./sql/schema"
        engine: "postgresql"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: false
        emit_exact_table_names: false
